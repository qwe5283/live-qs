{
  "name": "@live-qs/monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "check": "npm run check:server && npm run check:web && npm run check:windows && npm run check:android",
    "check:server": "npm --prefix server run typecheck && npm --prefix server test && npm --prefix server run build",
    "check:web": "npm --prefix web run build",
    "check:windows": "dotnet test windows\\LiveQs.Windows.Tests\\LiveQs.Windows.Tests.csproj && dotnet build windows\\LiveQs.Windows\\LiveQs.Windows.csproj -o windows\\artifacts\\verify",
    "check:android": "cd android && gradlew.bat lint test assembleDebug"
  }
}
