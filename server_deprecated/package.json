{
  "name": "@ai-life/server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/main.ts",
    "build:test": "tsc -p tsconfig.test.json",
    "test": "npm run build:test && node --test test/*.test.mjs",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@ai-life/shared": "file:../packages/shared"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.3"
  }
}
