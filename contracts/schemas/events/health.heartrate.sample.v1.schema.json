{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://live-qs.local/contracts/schemas/events/health.heartrate.sample.v1.schema.json",
  "title": "Health Heart Rate Sample Event V1",
  "description": "A source-provided instantaneous heart rate measurement. One event per sample; start_at is the measurement instant and the event carries no end_at because a heart rate sample has no duration.",
  "allOf": [
    {
      "$ref": "../event-envelope.v1.schema.json"
    }
  ],
  "type": "object",
  "properties": {
    "event_type": {
      "const": "health.heartrate.sample"
    },
    "schema_version": {
      "const": 1
    },
    "source": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "record_id"],
      "properties": {
        "kind": {
          "$ref": "#/$defs/SourceKind"
        },
        "record_id": {
          "type": "string",
          "minLength": 1,
          "description": "Stable record identifier supplied by Health Connect for the originating record; retained so source record counts reconcile against server acknowledgements."
        }
      }
    },
    "privacy_level": {
      "type": "string",
      "enum": ["normal", "sensitive"],
      "default": "sensitive",
      "description": "Health observations default to sensitive; credentials need an adequate privacy ceiling to upload or read them."
    },
    "payload": {
      "type": "object",
      "additionalProperties": false,
      "required": ["beats_per_minute", "data_origin"],
      "properties": {
        "beats_per_minute": {
          "type": "integer",
          "minimum": 15,
          "maximum": 300,
          "description": "Unit: beats per minute (bpm). Readings outside the plausible range are rejected as invalid instead of being rendered."
        },
        "data_origin": {
          "type": "string",
          "minLength": 1,
          "description": "Package name of the application that wrote the record into Health Connect. Observations from different origins stay distinct; a similar value from another origin never deletes this one."
        }
      }
    }
  },
  "$defs": {
    "SourceKind": {
      "title": "Source Kind",
      "type": "string",
      "description": "Health Connect records read by the Android collector. The origin application inside each payload is the data origin; the collector itself is only the transport.",
      "const": "android.healthconnect"
    }
  },
  "unevaluatedProperties": false
}
