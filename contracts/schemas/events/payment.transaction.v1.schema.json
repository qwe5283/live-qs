{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://live-qs.local/contracts/schemas/events/payment.transaction.v1.schema.json",
  "title": "Payment Transaction Event V1",
  "description": "A minimal structured transaction fact extracted on-device from a payment notification. start_at is the occurrence instant and the event carries no end_at because a transaction has no duration. Notification bodies, titles, and other free text never enter this contract: only the extracted amount, direction, approved merchant label, on-device rule category, and source metadata are uploaded.",
  "allOf": [
    {
      "$ref": "../event-envelope.v1.schema.json"
    }
  ],
  "type": "object",
  "properties": {
    "event_type": {
      "const": "payment.transaction"
    },
    "schema_version": {
      "const": 1
    },
    "source": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "record_id"],
      "properties": {
        "kind": {
          "$ref": "#/$defs/SourceKind"
        },
        "record_id": {
          "type": "string",
          "minLength": 1,
          "description": "Stable identifier of the originating source record (the notification), retained so source record counts reconcile against server acknowledgements. It identifies the notification, not a payment-network transaction; transactions without a network identifier that are suspected duplicates carry pending_confirmation instead of being merged."
        }
      }
    },
    "privacy_level": {
      "type": "string",
      "enum": ["normal", "sensitive"],
      "default": "sensitive",
      "description": "Financial observations default to sensitive; credentials need an adequate privacy ceiling to upload or read them."
    },
    "payload": {
      "type": "object",
      "additionalProperties": false,
      "required": ["amount", "direction", "merchant", "category", "pending_confirmation"],
      "properties": {
        "amount": {
          "type": "object",
          "additionalProperties": false,
          "required": ["value", "currency"],
          "properties": {
            "value": {
              "type": "integer",
              "minimum": 1,
              "maximum": 10000000000,
              "description": "Exact amount in minor currency units (for CNY: fen), never a float, so sums reconcile without rounding."
            },
            "currency": {
              "type": "string",
              "pattern": "^[A-Z]{3}$",
              "description": "ISO 4217 alphabetic currency code in upper case."
            }
          }
        },
        "direction": {
          "type": "string",
          "enum": ["income", "expense"],
          "description": "Whether the transaction credited (income) or debited (expense) the Owner."
        },
        "merchant": {
          "type": "string",
          "minLength": 1,
          "maxLength": 80,
          "description": "Approved merchant label extracted on-device by the versioned extraction rule; never the notification text itself."
        },
        "category": {
          "type": "string",
          "enum": [
            "food",
            "transport",
            "shopping",
            "bills",
            "health",
            "education",
            "entertainment",
            "transfer",
            "uncategorized"
          ],
          "description": "Spending category produced by on-device classification rules over the merchant label."
        },
        "pending_confirmation": {
          "type": "boolean",
          "description": "True when the transaction lacks a stable payment-network identifier and is suspected to duplicate another observation (matching amount, direction, merchant, and nearby occurrence time). Suspected duplicates are kept as distinct, flagged observations and are never merged on amount and time alone; the Owner confirms or dismisses them."
        }
      }
    }
  },
  "$defs": {
    "SourceKind": {
      "title": "Source Kind",
      "type": "string",
      "description": "Payment notifications parsed by the Android collector; the collector extracts structured fields locally and the raw notification never leaves the device.",
      "const": "android.wechatpay"
    }
  },
  "unevaluatedProperties": false
}
